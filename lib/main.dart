import 'package:flutter/material.dart';

void main() {
  runApp(const SunnahApp());
}

class SunnahApp extends StatelessWidget {
  const SunnahApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
